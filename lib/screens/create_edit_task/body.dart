import 'package:flutter/material.dart';

class CreateEditBody extends StatelessWidget {
  const CreateEditBody({super.key});

  @override
  Widget build(BuildContext context) {

    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: [

      ],
    );

  }
}
